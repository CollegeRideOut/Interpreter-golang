# Sequential Apply Diagnostic

This report was generated by `TestSequentialApplyTestProgramDiagnostic`. Structural expectations are derived from the target snapshot for this integration diagnostic; independent hardcoded expectations live in `hardcoded_apply_golden_test.go`.

- Program: `TestProgram`
- Source: `HEAD~1:main.go`
- Target: `working tree:main.go`
- Edit count: `74`
- Final intermediate AST matches target shape: `true`

## Observed Issues

1. Edit `12` `INSERT *ast.FuncType` (`root.Decls[1].Body.List[0].X.Args[1].Type`): no structural change; ancestor edit 11 (root.Decls[1].Body.List[0].X.Args[1]) was already applied and may have realized this child
2. Edit `25` `INSERT *ast.BlockStmt` (`root.Decls[1].Body.List[0].X.Args[1].Body`): no structural change; ancestor edit 11 (root.Decls[1].Body.List[0].X.Args[1]) was already applied and may have realized this child
3. Edit `27` `INSERT *ast.BinaryExpr` (`root.Decls[1].Body.List[0].X.Args[1].Body.List[0].Cond`): no structural change; ancestor edit 26 (root.Decls[1].Body.List[0].X.Args[1].Body.List[0]) was already applied and may have realized this child
4. Edit `34` `INSERT *ast.BlockStmt` (`root.Decls[1].Body.List[0].X.Args[1].Body.List[0].Body`): no structural change; ancestor edit 26 (root.Decls[1].Body.List[0].X.Args[1].Body.List[0]) was already applied and may have realized this child
5. Edit `69` `DELETE *ast.Ident` (`root.Decls[0].Body.List[0].Lhs[0]`): no structural change; ancestor edit 68 (root.Decls[0].Body.List[0]) was already applied and may have realized this child
6. Edit `70` `DELETE *ast.BasicLit` (`root.Decls[0].Body.List[0].Rhs[0]`): no structural change; ancestor edit 68 (root.Decls[0].Body.List[0]) was already applied and may have realized this child
7. Edit `72` `DELETE *ast.Ident` (`root.Decls[0].Body.List[1].Lhs[0]`): no structural change; ancestor edit 71 (root.Decls[0].Body.List[1]) was already applied and may have realized this child
8. Edit `73` `DELETE *ast.Ident` (`root.Decls[0].Body.List[1].Rhs[0]`): no structural change; ancestor edit 71 (root.Decls[0].Body.List[1]) was already applied and may have realized this child

## Analysis

- **Final structural comparison passed:** after all 74 edits, the intermediate AST has the same node kinds, values, fields, child order, and indexes as the target AST. Identity/provenance metadata is intentionally excluded from this comparison.
- **Dependent delete no-ops:** edits 69, 70, 72, and 73 target children of deleted assignments. Their parent deletes already removed those children, so the desired structural result was already present when those child deletes ran.
- **Identity regression fixed:** insert edits no longer inherit `SourceGlobalID` from an old node occupying the same path, so an insert cannot be mistaken for an existing node of another kind.
- **Renderer is not used for the structural verdict:** the test compares exact intermediate-tree JSON shapes before best-effort source rendering.

## Trace

The exact before/after JSON for every edit is in `sequential_apply_trace.json`. `beforeShape` and `afterShape` remove identity/provenance fields and are used to detect real structural changes.
